# name: CI

# permissions: {}

# on:
#   push:
#   pull_request:
#   workflow_dispatch:

# env:
#   FOUNDRY_PROFILE: ci

# jobs:
#   foundry:
#     name: Foundry project
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#     steps:
#       - uses: actions/checkout@v5
#         with:
#           persist-credentials: false
#           submodules: recursive

#       - name: Install Foundry
#         uses: foundry-rs/foundry-toolchain@v1

#       - name: Show Forge version
#         run: forge --version

#       - name: Run Forge fmt
#         run: forge fmt --check

#       - name: Run Forge build
#         run: forge build --sizes

#       - name: Run Forge tests
#         run: forge test -vvv

#   cadence:
#     name: Cadence (Flow CLI)
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#     steps:
#       - uses: actions/checkout@v5
#         with:
#           persist-credentials: false

#       - name: Install Flow CLI
#         run: sudo sh -ci "$(curl -fsSL https://raw.githubusercontent.com/onflow/flow-cli/master/install.sh)"

#       - name: Show Flow CLI version
#         run: flow version

#       - name: Run Cadence tests
#         run: flow test
