// Optional: copy to `config.local.js` (gitignored) to override `config.sample.js` without editing the sample.
window.DEMO_CONFIG = Object.assign(window.DEMO_CONFIG || {}, {
  // cadenceDeployer: "0x…",
  // vaultIdentifier: "A.….sFlowToken.Vault",
  // keeperUInt256Ids: "1 2 3",
  // keeperMaxBridgeFlowFee: "25.0",
  // keeperFlowCliExtra: "--config-path flow.deploy.json",
});
